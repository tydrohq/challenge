# Tydro Challenge — environment.
# Copy to `.env` (`cp .env.example .env`) before running anything.
# ForkBase fails loudly naming any var that is unset.
#
# These are working, non-secret defaults (a public RPC + a pinned block). The hiring team
# may override INK_RPC_URL with a private/archive endpoint if the public one is rate-limited.

# Ink RPC endpoint (archive state required for the pinned block).
# Verified working (archive back past block 40M): rpc-gel / rpc-qnd / ink.drpc.org
INK_RPC_URL=https://rpc-gel.inkonchain.com

# Pinned fork block (recent, confirmed retrievable on all three public RPCs).
FORK_BLOCK=48050000

# The asset whose oracle the candidate repoints (Task 1). Default: WETH market.
ORACLE_MIGRATION_TARGET=0x4200000000000000000000000000000000000006

# Candidate EOA: anvil account 0 (default mnemonic). Public test key — NOT a secret.
# Used only against the local anvil fork; never against a live network.
CANDIDATE=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# ---- Position-book seeding (optional overrides) ----
# ForkBase.seedPositionBook() reads these; if a var is unset it falls back to the "seed"
# block in addresses.json, and fails loudly if that is missing/zero. Defaults below match
# addresses.json and work out of the box. Borrowers/LP are anvil accounts #1/#2/#3 (public
# test keys) — NOT the candidate's account 0, so the seeded book never collides with the
# candidate's own positions. Collateral is ORACLE_MIGRATION_TARGET.
SEED_BORROW_ASSET=0x2D270e6886d130D724215A266106e6832161EAEd   # USDC (deep, borrowing-enabled)
SEED_LP=0x70997970C51812dc3A010C7d01b50e0d17dc79C8              # anvil #1 (liquidity provider)
SEED_BORROWER_1=0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC      # anvil #2
SEED_BORROWER_2=0x90F79bf6EB2c4f870365E785982E1f101E93b906      # anvil #3
SEED_TARGET_HF_1=20000                                          # 2.00 — comfortable
SEED_TARGET_HF_2=11500                                          # 1.15 — near the edge
SEED_COLLATERAL_AMOUNT=10                                       # whole tokens of collateral each
